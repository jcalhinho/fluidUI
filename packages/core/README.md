# @fluidui/core

Core package scaffold for the fluidUI monorepo.

This package currently mirrors the existing engine implementation from `src/` at repository root so adapters can target `@fluidui/core` explicitly.

Planned next step:
- move source-of-truth fully into this package
- keep repository root package as a compatibility wrapper (or deprecate)
